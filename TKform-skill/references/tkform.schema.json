{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://tkform.local/tkform.schema.json",
  "title": "TK-Form project file",
  "description": "Project file for the TK-Form VS Code extension. Describes a Tkinter GUI (canvas, widgets, events, variables, menus, resources). This schema covers structural shape only; per-widget prop allow-lists, cross-references, duplicate-id checks, Python code compilation, and layout mixing are enforced by the engine validator (run `scripts/validate_project.py`).",
  "type": "object",
  "additionalProperties": false,
  "required": ["name", "canvasWidth", "canvasHeight", "widgets"],
  "allOf": [
    {
      "if": {
        "properties": { "schemaVersion": { "const": 4 } },
        "required": ["schemaVersion"]
      },
      "then": {
        "required": ["toolkit"],
        "not": { "required": ["tkTheme"] }
      }
    }
  ],
  "properties": {
    "schemaVersion": {
      "type": "integer",
      "enum": [1, 2, 3, 4],
      "description": "Schema version. The designer normalizes v1/v2/v3 files to v4 on save."
    },
    "name": {
      "type": "string",
      "minLength": 1,
      "description": "Project / window name. Used as the export filename base."
    },
    "canvasWidth": {
      "type": "integer",
      "minimum": 1,
      "description": "Initial Tkinter root window width in pixels. Also the design canvas width."
    },
    "canvasHeight": {
      "type": "integer",
      "minimum": 1,
      "description": "Initial Tkinter root window height in pixels. Also the design canvas height."
    },
    "widgets": {
      "type": "array",
      "description": "Widget instances. Each becomes a Tkinter widget in the generated Python. See references/widgets.md for the per-type prop allow-list.",
      "items": { "$ref": "#/definitions/widget" },
      "maxItems": 2000
    },
    "toolkit": {
      "$ref": "#/definitions/toolkit",
      "description": "Active UI toolkit and theme. Required for schema v4; omitted legacy projects normalize to tkinter."
    },
    "tkTheme": {
      "type": "string",
      "deprecated": true,
      "description": "Deprecated v1-v3 ttk theme name. Schema v4 stores this value in toolkit.theme."
    },
    "menuBar": {
      "oneOf": [
        { "type": "null" },
        { "$ref": "#/definitions/menuBar" }
      ],
      "description": "Top-level menu bar. null/omitted = no menu bar. See references/menus.md."
    },
    "rootBg": {
      "type": "string",
      "description": "Root window background color (e.g. '#ffffff'). Empty string = Tkinter default."
    },
    "rootResizable": {
      "type": "boolean",
      "description": "Whether the root window can be resized by the user."
    },
    "rootTitle": {
      "type": "string",
      "description": "Window title. Defaults to the project name when omitted."
    },
    "rootIconImageId": {
      "type": "string",
      "description": "Image resource id used as the window icon via root.iconphoto(). Must reference an entry in `resources`."
    },
    "rootMinWidth": {
      "type": "integer",
      "minimum": 1,
      "description": "Minimum window width passed to root.minsize(). Both rootMinWidth and rootMinHeight must be set for minsize to apply."
    },
    "rootMinHeight": {
      "type": "integer",
      "minimum": 1,
      "description": "Minimum window height passed to root.minsize(). Both rootMinWidth and rootMinHeight must be set for minsize to apply."
    },
    "rootPosition": {
      "type": "string",
      "enum": ["default", "screenCenter"],
      "description": "Startup window placement. 'default' lets the window manager decide; 'screenCenter' centers the window on the primary screen."
    },
    "variables": {
      "type": "array",
      "description": "Tk variable declarations (StringVar/IntVar/DoubleVar/BooleanVar). Widgets reference these by name via props.variable / props.textvariable. See references/variables.md.",
      "items": { "$ref": "#/definitions/tkVariable" },
      "maxItems": 500
    },
    "nonVisuals": {
      "type": "array",
      "description": "Non-visual components: Timer, FileDialog, ColorChooser, MessageBox, plus ttkbootstrap-only TtkMessagebox, Querybox, DatePickerDialog, ColorPickerDialog, ToastNotification, and ToolTip. See references/non-visuals.md.",
      "items": { "$ref": "#/definitions/nonVisual" },
      "maxItems": 500
    },
    "resources": {
      "type": "array",
      "description": "Image resources embedded as base64 data URLs. Widgets reference these by id via props.image. See references/resources.md.",
      "items": { "$ref": "#/definitions/resource" },
      "maxItems": 200
    },
    "animations": {
      "type": "array",
      "description": "Declarative widget animations compiled to Tkinter after() callbacks.",
      "items": { "$ref": "#/definitions/animation" },
      "maxItems": 500
    }
  },

  "definitions": {
    "toolkit": {
      "type": "object",
      "additionalProperties": false,
      "required": ["name", "theme"],
      "properties": {
        "name": {
          "type": "string",
          "enum": ["tkinter", "ttkbootstrap"]
        },
        "majorVersion": {
          "type": "integer"
        },
        "theme": {
          "type": "string",
          "minLength": 1
        }
      },
      "allOf": [
        {
          "if": {
            "properties": { "name": { "const": "ttkbootstrap" } },
            "required": ["name"]
          },
          "then": {
            "required": ["majorVersion"],
            "properties": { "majorVersion": { "const": 2 } }
          },
          "else": {
            "not": { "required": ["majorVersion"] }
          }
        }
      ]
    },

    "widgetToolkitProps": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "tkinter": {
          "type": "object",
          "additionalProperties": true
        },
        "ttkbootstrap": {
          "type": "object",
          "additionalProperties": false,
          "properties": {
            "bootstyle": { "type": "string" },
            "icon": { "type": "string" },
            "iconSize": { "type": "integer", "minimum": 8, "maximum": 128 },
            "iconOnly": { "type": "boolean" }
          }
        }
      }
    },

    "pythonIdentifier": {
      "type": "string",
      "pattern": "^[A-Za-z_][A-Za-z0-9_]*$",
      "description": "Must be a valid Python identifier (not a reserved keyword). The engine rejects keywords like 'class', 'for', etc."
    },

    "pythonIdentifierUnicode": {
      "type": "string",
      "pattern": "^[^0-9\\s][^\\s]*$",
      "description": "Must be a valid Python identifier. Unicode letters are allowed per PEP 3131 (e.g. 'save_settings', '保存设置', '저장하기'), but the name must be NFKC-stable and must not be a Python keyword; the engine validator enforces this authoritatively."
    },

    "widget": {
      "type": "object",
      "additionalProperties": false,
      "required": ["id", "type", "name"],
      "properties": {
        "id": {
          "type": "string",
          "minLength": 1,
          "description": "Stable unique id within the project. Used as parentId target and bindings target. Convention: '<type>-<slug>' e.g. 'frame-login', 'button-save'."
        },
        "type": {
          "type": "string",
          "enum": [
            "Button", "Label", "Entry", "Text", "Checkbutton", "Radiobutton",
            "Listbox", "Scale", "Frame", "LabelFrame", "Canvas", "PanedWindow",
            "TtkPanedWindow", "OptionMenu", "Spinbox", "Scrollbar", "Separator",
            "Notebook", "Toplevel", "Progressbar", "Combobox", "Treeview",
            "Sizegrip", "Menubutton", "Message", "DateEntry", "LabeledScale",
            "Meter", "Floodgauge", "Tableview", "ScrolledText", "ScrolledFrame"
          ],
          "description": "Widget type. Maps directly to a Tkinter widget class. ttk widgets (Notebook, Progressbar, Combobox, Treeview, Sizegrip, Separator, TtkPanedWindow) cannot use bg/fg/padx/pady/etc. See references/widgets.md."
        },
        "name": {
          "$ref": "#/definitions/pythonIdentifier",
          "description": "Becomes the Python attribute name on the generated UI object. Must be unique across widgets."
        },
        "parentId": {
          "oneOf": [
            { "type": "null" },
            { "type": "string", "minLength": 1 }
          ],
          "description": "Parent widget id, or null for direct children of the root window. The parent MUST be a container type (Frame, LabelFrame, Canvas, PanedWindow, TtkPanedWindow, Notebook, Toplevel, ScrolledFrame). See references/layout.md."
        },
        "x": { "type": "number", "description": "X position (place layout) or design hint (grid layout)." },
        "y": { "type": "number", "description": "Y position (place layout) or design hint (grid layout)." },
        "width": { "type": "integer", "minimum": 1 },
        "height": { "type": "integer", "minimum": 1 },
        "props": {
          "type": "object",
          "description": "Tkinter widget options. The allowed keys depend on `type`. IMPORTANT: a `command` key here must be a Python identifier reference (e.g. 'self.on_click'); for inline code use `events.command` instead. See references/widgets.md for the per-type allow-list.",
          "additionalProperties": true
        },
        "toolkitProps": {
          "$ref": "#/definitions/widgetToolkitProps",
          "description": "Provider-specific visual options. Both provider bags are preserved; only the active widget backend consumes its bag."
        },
        "locked": { "type": "boolean", "description": "Designer-only: locks the widget against accidental drag in the canvas. Has no effect on generated code." },
        "designer": {
          "type": "object",
          "additionalProperties": false,
          "properties": {
            "locked": { "type": "boolean" },
            "tabIndex": { "type": "integer", "minimum": 0, "description": "Designer-only: Notebook tab order hint." },
            "activeTab": { "type": "integer", "minimum": 0, "description": "Designer-only: which Notebook tab is selected in the editor." }
          }
        },
        "bindings": {
          "type": "object",
          "additionalProperties": false,
          "properties": {
            "xscrollcommand": { "type": "string", "deprecated": true, "description": "Legacy reverse binding: Scrollbar widget id for horizontal scrolling. New projects should set bindings.command on the Scrollbar instead." },
            "yscrollcommand": { "type": "string", "deprecated": true, "description": "Legacy reverse binding: Scrollbar widget id for vertical scrolling. New projects should set bindings.command on the Scrollbar instead." },
            "command": { "type": "string", "description": "For Scrollbar: target widget id being scrolled. Horizontal targets can be Text, Listbox, Entry, Treeview, or Canvas. Vertical targets can be Text, Listbox, Treeview, or Canvas." }
          },
          "description": "Scroll wiring. bindings.command on the Scrollbar is the canonical format; xscrollcommand and yscrollcommand are accepted only for legacy compatibility."
        },
        "events": {
          "description": "Event handlers. Two forms are both accepted. MAP form: { 'command': 'print(1)' } or { '<Leave>': { handlerName, code } }. ARRAY form: [{ id, event, kind, handlerName, code, enabled }]. See references/events.md.",
          "oneOf": [
            { "type": "object" },
            { "type": "array", "items": { "$ref": "#/definitions/eventHandler" } }
          ]
        },
        "layoutManager": {
          "type": "string",
          "enum": ["place", "grid", "pack"],
          "description": "Per-widget layout manager. Children of the SAME parent must all use the same manager, EXCEPT children of Toplevel, PanedWindow, TtkPanedWindow. Omitting implies 'place'. See references/layout.md."
        },
        "gridRow": { "type": "integer", "minimum": 0 },
        "gridCol": { "type": "integer", "minimum": 0 },
        "gridRowSpan": { "type": "integer", "minimum": 1 },
        "gridColSpan": { "type": "integer", "minimum": 1 },
        "gridSticky": { "type": "string", "description": "Tkinter sticky spec, e.g. 'nsew', 'ew', 'n+s'." },
        "gridPadX": { "type": "number", "minimum": 0 },
        "gridPadY": { "type": "number", "minimum": 0 },
        "gridIPadX": { "type": "number", "minimum": 0 },
        "gridIPadY": { "type": "number", "minimum": 0 },
        "gridRowWeight": {
          "type": "integer",
          "minimum": 0,
          "description": "Weight for this widget's grid row via the parent's rowconfigure(). 0 (default) = the row does not stretch. Per row, the maximum weight across siblings wins."
        },
        "gridColWeight": {
          "type": "integer",
          "minimum": 0,
          "description": "Weight for this widget's grid column via the parent's columnconfigure(). 0 (default) = the column does not stretch. Per column, the maximum weight across siblings wins."
        },
        "packSide": { "type": "string", "enum": ["top", "bottom", "left", "right"], "description": "pack() side. Default 'top'." },
        "packFill": { "type": "string", "enum": ["none", "x", "y", "both"], "description": "pack() fill. Default 'none'." },
        "packExpand": { "type": "boolean", "description": "pack() expand. Default false." },
        "packPadX": { "type": "number", "minimum": 0 },
        "packPadY": { "type": "number", "minimum": 0 },
        "packAnchor": { "type": "string", "enum": ["n", "ne", "e", "se", "s", "sw", "w", "nw", "center"] }
      }
    },

    "eventHandler": {
      "type": "object",
      "additionalProperties": false,
      "required": ["event"],
      "properties": {
        "id": { "type": "string", "description": "Stable unique id, e.g. 'event-save'." },
        "event": {
          "type": "string",
          "description": "Tk event name. 'command' (button/selection command) or a Tk bind sequence such as '<Leave>', '<Button-1>', '<Key>'."
        },
        "kind": { "type": "string", "enum": ["command", "bind"], "description": "Ignored — derived from `event` (event=='command' => kind='command')." },
        "handlerName": {
          "$ref": "#/definitions/pythonIdentifierUnicode",
          "description": "Generated Python method name. Unicode identifiers (PEP 3131) such as '保存设置' are allowed. If omitted, an anonymous handler is generated. Must be unique across ALL enabled handlers in the project."
        },
        "code": { "type": "string", "description": "Python code body. Compiled with `def <handlerName>(event=None): <code>`; must be syntactically valid. May reference Tk variables by name (e.g. 'filter_var.get()')." },
        "enabled": { "type": "boolean", "description": "If false, the handler is skipped at codegen time." }
      }
    },

    "animation": {
      "type": "object",
      "additionalProperties": false,
      "required": ["id", "name", "targetWidgetId", "preset", "trigger", "durationMs", "delayMs", "easing", "repeatCount", "parameters"],
      "properties": {
        "id": { "type": "string", "minLength": 1 },
        "name": { "$ref": "#/definitions/pythonIdentifier" },
        "targetWidgetId": { "type": "string", "minLength": 1 },
        "preset": { "type": "string", "enum": ["slide", "shake", "bounce", "pulse", "color", "fill", "grow"] },
        "trigger": { "type": "string", "enum": ["load", "click", "hoverEnter", "hoverLeave", "focusIn", "focusOut", "manual"] },
        "durationMs": { "type": "number", "exclusiveMinimum": 0, "maximum": 600000 },
        "delayMs": { "type": "number", "minimum": 0, "maximum": 86400000 },
        "easing": { "type": "string", "enum": ["linear", "easeIn", "easeOut", "easeInOut", "backOut", "elasticOut", "bounceOut"] },
        "repeatCount": {
          "oneOf": [
            { "type": "integer", "minimum": 1, "maximum": 10000 },
            { "const": "infinite" }
          ]
        },
        "parameters": { "type": "object" }
      },
      "allOf": [
        {
          "if": {
            "properties": { "preset": { "const": "slide" } },
            "required": ["preset"]
          },
          "then": {
            "properties": { "parameters": { "$ref": "#/definitions/slideAnimationParameters" } }
          }
        },
        {
          "if": {
            "properties": { "preset": { "const": "shake" } },
            "required": ["preset"]
          },
          "then": {
            "properties": { "parameters": { "$ref": "#/definitions/shakeAnimationParameters" } }
          }
        },
        {
          "if": {
            "properties": { "preset": { "const": "bounce" } },
            "required": ["preset"]
          },
          "then": {
            "properties": { "parameters": { "$ref": "#/definitions/bounceAnimationParameters" } }
          }
        },
        {
          "if": {
            "properties": { "preset": { "const": "pulse" } },
            "required": ["preset"]
          },
          "then": {
            "properties": { "parameters": { "$ref": "#/definitions/pulseAnimationParameters" } }
          }
        },
        {
          "if": {
            "properties": { "preset": { "const": "color" } },
            "required": ["preset"]
          },
          "then": {
            "properties": { "parameters": { "$ref": "#/definitions/colorAnimationParameters" } }
          }
        },
        {
          "if": {
            "properties": { "preset": { "const": "fill" } },
            "required": ["preset"]
          },
          "then": {
            "properties": { "parameters": { "$ref": "#/definitions/fillAnimationParameters" } }
          }
        },
        {
          "if": {
            "properties": { "preset": { "const": "grow" } },
            "required": ["preset"]
          },
          "then": {
            "properties": { "parameters": { "$ref": "#/definitions/growAnimationParameters" } }
          }
        }
      ]
    },

    "slideAnimationParameters": {
      "type": "object",
      "additionalProperties": true,
      "required": ["direction", "distance"],
      "properties": {
        "direction": { "type": "string", "enum": ["left", "right", "up", "down"] },
        "distance": { "type": "number", "minimum": 0, "maximum": 1000000 }
      }
    },

    "shakeAnimationParameters": {
      "type": "object",
      "additionalProperties": true,
      "required": ["axis", "distance", "cycles"],
      "properties": {
        "axis": { "type": "string", "enum": ["x", "y"] },
        "distance": { "type": "number", "minimum": 0, "maximum": 1000000 },
        "cycles": { "type": "integer", "minimum": 1, "maximum": 10000 }
      }
    },

    "bounceAnimationParameters": {
      "type": "object",
      "additionalProperties": true,
      "required": ["direction", "distance", "cycles"],
      "properties": {
        "direction": { "type": "string", "enum": ["left", "right", "up", "down"] },
        "distance": { "type": "number", "minimum": 0, "maximum": 1000000 },
        "cycles": { "type": "integer", "minimum": 1, "maximum": 10000 }
      }
    },

    "pulseAnimationParameters": {
      "type": "object",
      "additionalProperties": true,
      "required": ["scale"],
      "properties": {
        "scale": { "type": "number", "exclusiveMinimum": 0, "maximum": 1000 }
      }
    },

    "colorAnimationParameters": {
      "type": "object",
      "additionalProperties": true,
      "required": ["property", "to"],
      "properties": {
        "property": { "type": "string", "enum": ["bg", "fg"] },
        "to": { "type": "string", "pattern": "^#(?:[0-9A-Fa-f]{3}|[0-9A-Fa-f]{6})$" }
      }
    },

    "fillAnimationParameters": {
      "type": "object",
      "additionalProperties": true,
      "required": ["to"],
      "properties": {
        "from": { "type": "number", "description": "Start value. Omitted = the widget's current value when the animation starts." },
        "to": { "type": "number", "description": "End value written to the widget's value option (Progressbar/Floodgauge)." }
      }
    },

    "growAnimationParameters": {
      "type": "object",
      "additionalProperties": true,
      "description": "No parameters. The widget grows from zero to its designed size, centered."
    },

    "tkVariable": {
      "type": "object",
      "additionalProperties": false,
      "required": ["id", "name", "varType"],
      "properties": {
        "id": { "type": "string", "minLength": 1 },
        "name": {
          "$ref": "#/definitions/pythonIdentifier",
          "description": "Python attribute name. Widgets reference this via props.variable / props.textvariable."
        },
        "varType": {
          "type": "string",
          "enum": ["StringVar", "IntVar", "DoubleVar", "BooleanVar"]
        },
        "defaultValue": {
          "type": "string",
          "description": "Initial value as a string. The engine casts to the var type. For BooleanVar use '1'/'0' or 'True'/'False'."
        }
      }
    },

    "nonVisual": {
      "type": "object",
      "additionalProperties": false,
      "required": ["id", "type", "name"],
      "properties": {
        "id": { "type": "string", "minLength": 1 },
        "type": {
          "type": "string",
          "enum": ["Timer", "FileDialog", "ColorChooser", "MessageBox", "TtkMessagebox", "Querybox", "DatePickerDialog", "ColorPickerDialog", "ToastNotification", "ToolTip"],
          "description": "See references/non-visuals.md. FileDialog has a special `filetypes` prop parsing rule. TtkMessagebox, Querybox, DatePickerDialog, ColorPickerDialog, ToastNotification, and ToolTip require toolkit ttkbootstrap."
        },
        "name": { "$ref": "#/definitions/pythonIdentifier" },
        "props": {
          "type": "object",
          "additionalProperties": true,
          "description": "Type-specific configuration. See references/non-visuals.md."
        },
        "events": {
          "description": "Same shape as widget events. Timer typically binds 'command' (tick). See references/events.md.",
          "oneOf": [
            { "type": "object" },
            { "type": "array", "items": { "$ref": "#/definitions/eventHandler" } }
          ]
        }
      }
    },

    "resource": {
      "type": "object",
      "additionalProperties": false,
      "required": ["id", "name"],
      "properties": {
        "id": { "type": "string", "minLength": 1 },
        "name": {
          "type": "string",
          "minLength": 1,
          "description": "Display name; converted to a Python identifier for the resource attribute. Avoid spaces/symbols."
        },
        "type": { "type": "string", "const": "image" },
        "dataUrl": {
          "type": "string",
          "description": "Base64 data URL ('data:image/png;base64,...'). Decoded payload must be <= 5MB and valid base64."
        }
      }
    },

    "menuBar": {
      "type": "object",
      "additionalProperties": false,
      "required": ["menus"],
      "properties": {
        "menus": {
          "type": "array",
          "items": { "$ref": "#/definitions/menu" },
          "description": "Top-level menus, left to right."
        }
      }
    },

    "menu": {
      "type": "object",
      "additionalProperties": false,
      "required": ["id", "items"],
      "properties": {
        "id": { "type": "string", "minLength": 1 },
        "label": { "type": "string", "description": "Menu label as shown on the menu bar (e.g. 'File', 'Edit')." },
        "items": {
          "type": "array",
          "items": { "$ref": "#/definitions/menuItem" },
          "description": "Items in display order. Cascade items contain nested `children`."
        }
      }
    },

    "menuItem": {
      "type": "object",
      "additionalProperties": false,
      "required": ["id"],
      "properties": {
        "id": { "type": "string", "minLength": 1 },
        "label": { "type": "string", "description": "Display label. Required for non-separator items." },
        "kind": {
          "type": "string",
          "enum": ["command", "separator", "checkbutton", "radiobutton", "cascade"],
          "description": "Item behavior. 'cascade' items have non-empty `children`. 'separator' renders a horizontal rule. Defaults to 'command'."
        },
        "separator": { "type": "boolean", "description": "Shorthand: true forces kind='separator'." },
        "accelerator": { "type": "string", "description": "Display-only hint shown on the right (e.g. 'Ctrl+R')." },
        "acceleratorBinding": { "type": "string", "description": "Tk bind sequence that actually triggers the item (e.g. '<Control-r>'). Bound on root." },
        "variable": {
          "$ref": "#/definitions/pythonIdentifier",
          "description": "For checkbutton/radiobutton: the Tk variable to bind. Must be declared in `variables`."
        },
        "value": { "type": "string", "description": "For radiobutton: the value this choice writes to `variable`." },
        "events": {
          "description": "Handlers. For 'command' items the 'command' event fires on click.",
          "oneOf": [
            { "type": "object" },
            { "type": "array", "items": { "$ref": "#/definitions/eventHandler" } }
          ]
        },
        "children": {
          "type": "array",
          "items": { "$ref": "#/definitions/menuItem" },
          "description": "Submenu items (only meaningful when kind='cascade'). Subject to the same max nesting depth (64) as widgets."
        }
      }
    }
  }
}
